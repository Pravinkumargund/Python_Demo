# Create an app for using if else check person is eligible or not for voting.
age = int(input("Enter your age: "))
if (age >= 18):
    print("Eligible for voting")
else:
    print("Not Eligible for voting")
